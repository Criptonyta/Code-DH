
// var = Declarando una variable una variable y asignandole un valor de string

var nombre = 'Nacho';
console.log (nombre);

// let = Diferencias entre let y var (!= nombre en el mismo bloque de ejecucion, entre {})

let numero_preferido = 3;
console.log (numero_preferido);

if (true) {
    let numero_preferido = 8;
    console.log(numero_preferido);
}

// const = (tiene las mismas particularidades que let pero no le podremos cambiar el valor)

const apellido = 'Navaza';
console.log(apellido);