const express = require('express');
const app = express();

app.listen(3000, ()=>{
    console.log('Servidor funcionando');
    });

const router = express.Router();




// app.use('/about', main);
// app.use('/',main);
    